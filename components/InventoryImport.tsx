'use client';
import {useState} from 'react';
import * as XLSX from 'xlsx';
type Row=Record<string,unknown>;
export default function InventoryImport(){const[rows,setRows]=useState<Row[]>([]),[message,setMessage]=useState(''),[importing,setImporting]=useState(false);async function read(file:File){setMessage('');const data=await file.arrayBuffer();const wb=XLSX.read(data);const ws=wb.Sheets[wb.SheetNames[0]];const parsed=XLSX.utils.sheet_to_json<Row>(ws,{defval:''}).slice(0,1000);setRows(parsed);setMessage(`${parsed.length} Zeilen erkannt. Bitte Vorschau prüfen.`)}async function run(){setImporting(true);let ok=0,failed=0;for(const row of rows){const normalized=Object.fromEntries(Object.entries(row).map(([k,v])=>[k.trim().toLowerCase(),v]));const payload={sku:String(normalized.sku||''),brand:String(normalized.marke||normalized.brand||''),category:String(normalized.kategorie||normalized.category||''),subcategory:String(normalized.unterkategorie||normalized.subcategory||''),original_size:String(normalized.größe||normalized.groesse||normalized.original_size||''),sale_price:normalized.verkaufspreis?Number(normalized.verkaufspreis):null,status:String(normalized.status||'Entwurf'),warehouse_location:String(normalized.lagerort||'')};if(!payload.sku){failed++;continue}const r=await fetch('/api/products',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});r.ok?ok++:failed++}setMessage(`Import abgeschlossen: ${ok} erfolgreich, ${failed} übersprungen/fehlerhaft.`);setImporting(false)}return <section className="capture-card import-card"><div className="capture-heading"><div><span className="step-badge">+</span><h2>Excel-/CSV-Bestand importieren</h2></div></div><p>Bis zu 1.000 Zeilen. Unterstützte Spalten: SKU, Marke, Kategorie, Unterkategorie, Größe, Verkaufspreis, Status, Lagerort.</p><input type="file" accept=".xlsx,.xls,.csv" onChange={e=>e.target.files?.[0]&&void read(e.target.files[0])}/>{rows.length>0&&<><div className="table-wrap"><table><thead><tr>{Object.keys(rows[0]).slice(0,8).map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{rows.slice(0,5).map((row,i)=><tr key={i}>{Object.values(row).slice(0,8).map((v,j)=><td key={j}>{String(v)}</td>)}</tr>)}</tbody></table></div><button className="primary-button centered-button" onClick={run} disabled={importing}>{importing?'Import läuft …':'Geprüfte Zeilen importieren'}</button></>}{message&&<div className="form-message success">{message}</div>}</section>}
