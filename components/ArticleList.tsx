'use client';
import Link from 'next/link';
import { useCallback,useEffect,useMemo,useState } from 'react';
type ImageRow={public_url:string;sort_order:number};type Product={id:string;sku:string;brand?:string;category?:string;subcategory?:string;original_size?:string;status?:string;sale_price?:number|null;occasions?:string[];product_images?:ImageRow[]};
export default function ArticleList({inventoryMode=false}:{inventoryMode?:boolean}){const[items,setItems]=useState<Product[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[query,setQuery]=useState(''),[status,setStatus]=useState('Alle'),[category,setCategory]=useState('Alle');const load=useCallback(async()=>{setLoading(true);setError('');try{const r=await fetch(`/api/products?ts=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw new Error(await r.text());setItems(await r.json())}catch(e){setError(e instanceof Error?e.message:'Ladefehler')}finally{setLoading(false)}},[]);useEffect(()=>{void load()},[load]);const cats=useMemo(()=>Array.from(new Set(items.map(x=>x.category).filter(Boolean) as string[])).sort(),[items]);const stats=useMemo(()=>Array.from(new Set(items.map(x=>x.status||'Entwurf'))).sort(),[items]);const filtered=useMemo(()=>items.filter(x=>{const h=[x.sku,x.brand,x.category,x.subcategory,...(x.occasions||[])].filter(Boolean).join(' ').toLowerCase();return(!query||h.includes(query.toLowerCase()))&&(status==='Alle'||(x.status||'Entwurf')===status)&&(category==='Alle'||x.category===category)}),[items,query,status,category]);if(loading)return <section className="panel empty-state"><h2>Artikel werden geladen …</h2></section>;if(error)return <section className="panel empty-state"><h2>Artikel konnten nicht geladen werden</h2><p>{error}</p><button className="primary-button centered-button" onClick={()=>void load()}>Erneut laden</button></section>;return <><section className="catalog-toolbar panel"><div className="catalog-summary"><strong>{items.length}</strong><span>{inventoryMode?'Artikel im Gesamtlager':'gespeicherte Artikel'}</span></div><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="SKU, Marke, Kategorie oder Anlass suchen"/><select value={status} onChange={e=>setStatus(e.target.value)}><option>Alle</option>{stats.map(x=><option key={x}>{x}</option>)}</select><select value={category} onChange={e=>setCategory(e.target.value)}><option>Alle</option>{cats.map(x=><option key={x}>{x}</option>)}</select><button className="secondary-button refresh-button" onClick={()=>void load()}>Aktualisieren</button></section>{filtered.length===0?<section className="panel empty-state"><h2>Keine passenden Artikel</h2></section>:<section className="article-list-grid">{filtered.map(item=>{const image=[...(item.product_images||[])].sort((a,b)=>a.sort_order-b.sort_order)[0];return <Link href={`/articles/${item.id}`} className="inventory-card clickable-card" key={item.id}>{image?<img src={image.public_url} alt="Artikel"/>:<div className="inventory-placeholder">MON CHIC</div>}<div className="inventory-card-body"><span className="inventory-sku">{item.sku}</span><h2>{[item.brand,item.subcategory||item.category].filter(Boolean).join(' · ')||'Artikel'}</h2><p className="inventory-meta">{[item.category,item.original_size?`Größe ${item.original_size}`:null].filter(Boolean).join(' · ')}</p><p>{item.occasions?.slice(0,3).join(' · ')||'Keine Anlässe ausgewählt'}</p><footer><strong>{item.sale_price!=null?`${Number(item.sale_price).toFixed(2).replace('.',',')} €`:'Preis offen'}</strong><span className="status-pill">{item.status||'Entwurf'}</span></footer><span className="card-action">Öffnen & bearbeiten →</span></div></Link>})}</section>}</>}
